on: [push]

jobs:
  xccdf-validate:
    runs-on: ubuntu-latest
    name: Validate xccdf
    steps:
      - uses: actions/checkout@master

      - name: Validate XCCDF File
        uses: mitre/inspec-profile-update-action@v1
